plugins:
  riverpod_lint: ^3.1.4

include:
  - package:openfoodfacts_flutter_lints/flutter.yaml
  - package:very_good_analysis/analysis_options.yaml
  - package:lint/strict.yaml

linter:
  rules:
    # ----- Disabled (not applicable to Flutter apps) -----
    avoid_slow_async_io: false

    # ----- Disabled (very_good_analysis style rules — too much churn) -----
    always_specify_types: false
    always_put_control_body_on_new_line: false

    # ----- Disabled (conflicts with multi-line string building) -----
    prefer_adjacent_string_concatenation: false
    no_adjacent_strings_in_list: false
    missing_whitespace_between_adjacent_strings: false

    # ----- Disabled (Riverpod notifiers use methods, not setters) -----
    use_setters_to_change_properties: false

    # ----- Disabled (Flutter SDK version skew) -----
    # unnecessary_unawaited fires only on newer SDKs whose APIs are
    # annotated @awaitNotRequired (e.g. Navigator.push in Flutter 3.47);
    # the project's pinned local SDK (3.12) lacks the annotation, so
    # removing unawaited() here would break the local discarded_futures
    # gate. Keeping the explicit unawaited() calls satisfies both.
    unnecessary_unawaited: false

    # ----- Error prevention -----
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_print: true
    cancel_subscriptions: true
    close_sinks: true
    empty_statements: true
    no_duplicate_case_values: true
    throw_in_finally: true
    unnecessary_statements: true

    # ----- Style & consistency -----
    always_put_required_named_parameters_first: true
    annotate_overrides: true
    avoid_function_literals_in_foreach_calls: true
    avoid_init_to_null: true
    avoid_redundant_argument_values: true
    avoid_relative_lib_imports: true
    avoid_unnecessary_containers: true
    curly_braces_in_flow_control_structures: true
    directives_ordering: true
    flutter_style_todos: true
    join_return_with_assignment: true
    lines_longer_than_80_chars: true
    prefer_collection_literals: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_locals: true
    prefer_if_null_operators: true
    prefer_initializing_formals: true
    prefer_interpolation_to_compose_strings: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    prefer_null_aware_operators: true
    prefer_single_quotes: true
    sort_child_properties_last: true
    sort_constructors_first: true
    type_annotate_public_apis: true
    unawaited_futures: true
    use_build_context_synchronously: true
    use_key_in_widget_constructors: true
    use_super_parameters: true

analyzer:
  errors:
    missing_return: error
    missing_required_param: error
    must_be_immutable: warning
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.gr.dart"
    - "**app_localizations*.dart"
    - "**firebase_options.dart"
    - "tools/**"
